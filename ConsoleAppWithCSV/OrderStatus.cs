namespace ConsoleAppWithCSV
{
    /// <summary>
    /// OrderStatus is a Enum which holds the status of the order by users
    /// </summary>
    public enum OrderStatus
    {
                unKnown,Ordered,Cancelled
    }
}